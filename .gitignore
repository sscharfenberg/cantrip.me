*.log
.DS_Store
.env
.env.production
.env.backup
.env.staging
.phpactor.json
.phpunit.result.cache

/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json

/coverage
/node_modules

# Playwright — the parked E2E sessions, its per-run artefacts, and its HTML report.
/tests/e2e/.auth
/test-results
/playwright-report

# JUnit XML and the shields.io endpoint documents built from it. CI makes both and
# publishes the badges to the `badges` branch; locally they are scratch output.
/reports
/badges

/public/build
/public/hot
/public/hot.e2e-backup
/public/storage
/public/art-crops
/public/card-images
/public/set
/public/set-icon
/public/symbol

/storage/*.key
/storage/pail
/vendor

Homestead.json
Homestead.yaml
Thumbs.db
tsconfig.tsbuildinfo

.mcp.json
boost.json
/.claude
